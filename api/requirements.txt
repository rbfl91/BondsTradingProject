flask==3.1.3
flask-cors==6.0.5
# web3 7.x: the 6.15 pin was uninstallable on Python 3.13 (its transitive
# lru-dict<1.3 / ckzg<2 constraints have no cp313 wheels and build from source).
web3==7.16.0
python-dotenv==1.2.3
requests==2.34.2
# gunicorn is Unix-only (production target: the Linux container — see
# api/Dockerfile). Local Windows dev uses `python app.py` instead.
gunicorn==26.0.0
# N-17: safe parsing of external XML (CoinDesk RSS) — entity-expansion hygiene
defusedxml==0.7.1
pyyaml==6.0.3
openapi-spec-validator==0.9.0
